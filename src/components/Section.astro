---
interface Props {
  title?: string;
  id?: string;
  /** Narrow content width for readability */
  narrow?: boolean;
  class?: string;
}

const { title, id, narrow = false, class: className = '' } = Astro.props;
---

<section id={id} class={`py-12 md:py-16 ${className}`} aria-labelledby={title ? `${id || 'section'}-heading` : undefined}>
  <div class:list={['mx-auto px-4 sm:px-6', narrow ? 'max-w-2xl' : 'max-w-4xl']}>
    {title && (
      <h2 id={`${id || 'section'}-heading`} class="font-heading text-2xl md:text-3xl font-semibold text-[var(--color-ink)] mb-6">
        {title}
      </h2>
    )}
    <slot />
  </div>
</section>
